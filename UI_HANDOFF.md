# PageTurn — UI Handoff Notes

## Admin Page Mockups Required

The following admin pages are referenced in the Loom walkthrough script (Scene 6) and in the FRONTEND.md spec but **do not yet have HTML mockups**:

- `/admin` — Admin Dashboard (stat cards, recent activity)
- `/admin/books` — Admin Books Page (search, table, add/edit modal, **CopyManagementModal**)
- `/admin/users` — Admin Users Page (search, table)
- `/admin/users/:id` — Admin User Detail (profile, tabbed sections, waive buttons)
- `/admin/fines` — Admin Fines Page (filter tabs, table, waive actions)

**Action required**: Create HTML mockups for these pages before recording the Loom walkthrough. The implementation agent can build these pages from the FRONTEND.md spec alone, but the Loom recording needs visual reference for consistency with the existing mockups.

## Existing Mockups

- `mockups/option-c4-home.html` — Homepage (browse + search)
- `mockups/book-detail.html` — Book detail page
- `mockups/loans.html` — My Loans page
- `mockups/history.html` — Loan History page
- `mockups/fines.html` — Fines page
- `mockups/settings.html` — Settings / API Keys page
